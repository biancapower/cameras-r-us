<%= form_with(model: retailer) do |form| %>
  <% if retailer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(retailer.errors.count, "error") %> prohibited this retailer from being saved:</h2>

      <ul>
        <% retailer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field" data-controller="address" data-action="map-loaded@window->address#initGoogle">
    <%= form.label :address %>
    <%= form.search_field :address, "data-address-target" => "input" %>
  </div>

  <div class="field">
    <%= form.label :maps_url %>
    <%= form.text_field :maps_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
